Importing a module as main
==========================

The code is not written yet. Explain what happens when
we wish to import a module as the main module - in
spite of having to import another script first
to enable an import hook.
