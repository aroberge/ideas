"""tortue.pyfr"""

de turtle importer color, fd, left, pen

pen(pensize=3)

répéter 4:   # French ...
    repeat 4:  # or English ... both work.
        repeat 4:
            color('blue')
            fd(30)
            left(-90)
        color('green')
        fd(60)
        left(90)
    color('red')
    left(90)
    fd(90)


input("Press any key to quit.")
